export const initialState = {
  error: '',
  isFetching: false,
  universities: []
};

export const reducer = (state = initialState, {type, payload}) => {
  switch (type) {
    default:
      return state;
  }
};
